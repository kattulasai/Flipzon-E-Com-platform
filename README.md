# React E-Mart

Simple React e-commerce demo using FakeStore API.

Run locally:

```bash
npm install
npm run dev
```

Open http://localhost:5173

Features: search, add to cart, wishlist, product detail, localStorage persistence.
